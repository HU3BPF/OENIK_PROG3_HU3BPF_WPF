<Window x:Class="Shops.WpfClient.View.NonCruds.GetShopAveragePriceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Shops.WpfClient.Model.NonCruds"
        mc:Ignorable="d"
        Title="GetShopAveragePriceWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:GetShopAveragePriceViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <ListBox Grid.Column="0"  ItemsSource="{Binding GetShopAveragePrices}" SelectedItem="{Binding SelectedShopAveragePrice}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding ShopName}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Column="1">
            <Label Content="Shop Name" HorizontalContentAlignment="Center" Background="LightBlue" Padding="10" />
            <TextBlock Text="{Binding SelectedShopAveragePrice.ShopName}" Padding="10" Margin="0,0,0,30" />
            <Label Content="Average Price" HorizontalContentAlignment="Center" Background="LightBlue" Padding="10" />
            <TextBlock Padding="10" Margin="0,0,0,30">
                <TextBlock.Text>
                    <MultiBinding StringFormat=" {0}$">
                        <Binding Path="SelectedShopAveragePrice.AveragePrice"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            <Label Content="{Binding ErrorMessage}" FontSize="18" Foreground="Red" HorizontalContentAlignment="Center" />
        </StackPanel>
    </Grid>
</Window>
